@model IEnumerable<Irimies_Mircea_Proiect_Medii_de_Programare.Models.Conferinta>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.nume_divizie)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.nume_divizie)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ConferintaID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ConferintaID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ConferintaID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
